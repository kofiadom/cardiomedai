// Configuration values
const config = {
    API_BASE_URL: 'https://cardiomedai-api.onrender.com',  // Local development server
    DEFAULT_USER_ID: 1  // Demo user ID
};

// Make config available globally
window.config = config;
